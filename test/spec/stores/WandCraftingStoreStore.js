'use strict';

describe('WandCraftingStoreStore', () => {
  let store;

  beforeEach(() => {
    store = require('stores/WandCraftingStoreStore.js');
  });

  it('should be defined', () => {
    expect(store).toBeDefined();
  });
});
