'use strict';

describe('CraftingActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/CraftingActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
